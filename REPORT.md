# BacktestLab 项目汇报

## 一、项目简介

**BacktestLab** 是一个面向中国 A 股市场的 AI 驱动量化回测平台。从数据获取、技术指标计算、策略信号生成、到回测撮合和结果可视化，提供完整的量化研究流水线。

- 🌐 **在线地址**：http://39.106.56.174:8080
- 📦 **GitHub**：https://github.com/huangazazaz/BacktestLab.git

---

## 二、核心功能展示

### 2.1 功能全景

```
┌──────────────────────────────────────────────────────────────┐
│                      BacktestLab                              │
├────────────┬────────────┬────────────┬───────────┬───────────┤
│  📊 单股回测 │  📈 批量回测 │  🎯 组合回测 │ 🔍 智能选股 │ 🤖 AI策略 │
│  K线权益图  │  排行榜    │  多标的同时 │ 选股器海选 │ 自然语言  │
│  交易明细   │  分组回测  │  四维出场   │ 策略买点   │ →Python   │
│  参数调优   │  柱状图    │  市场感知   │ 因子透明  │ 即时回测  │
├────────────┴────────────┴────────────┴───────────┴───────────┤
│  🔐 JWT鉴权  │  📁 分组管理  │  📡 K线查询  │  ⏰ 定时任务    │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 8 个功能模块

| 模块         | 说明                           | 亮点                  |
| ------------ | ------------------------------ | --------------------- |
| **仪表盘**   | Landing Page，6 张功能卡片直达 | 渐变色 Hero + CTA     |
| **智能选股** | 选股器海选 + 策略买点确认      | 因子评分透明化        |
| **单股分析** | 单股票 + 单策略深度回测        | K 线权益图 + 买卖标记 |
| **批量回测** | 多股票同时回测，横向对比       | 排行榜 + 金银铜牌     |
| **组合回测** | 真实账户模拟，多标的持仓       | 四维出场 + 权益曲线图 |
| **AI 策略**  | 自然语言生成策略代码           | 多轮对话打磨          |
| **K 线数据** | 原始 OHLCV 数据查询            | 交互式 K 线图         |
| **分组管理** | 股票分组 CRUD                  | 内置 16 个行业分组    |

### 2.3 技术架构

```
触发层：  CLI (Click)  │  Web API (FastAPI)  │  定时调度 (APScheduler)
                         ↓
编排层：  analyze_stock() │ run_backtest() │ run_portfolio_backtest() │ run_screen()
                         ↓
核心层：  DataSource → Indicator → Strategy → Screener → Backtester → Reporter
          (全部插件化，自动发现)                     (唯一内置，A股硬约束)
```

### 2.4 关键数字

| 指标            | 数值                                    |
| --------------- | --------------------------------------- |
| 内置策略        | 9 个（均线交叉、MACD背离、海龟、游资…） |
| 技术指标        | 6 个（MA/EMA/MACD/RSI/布林/ATR）        |
| 数据源          | 3 个（AKShare / Tushare / 本地缓存）    |
| 选股器          | 2 个（7因子动量 / 游资打板）            |
| Python 源文件   | 58 个                                   |
| React/TS 源文件 | 30 个                                   |
| 测试文件        | 25 个                                   |
| YAML 配置       | 30 个                                   |
| **总代码量**    | **~28,000 行**                          |

---

## 三、Vibecoding 实战：如何高效利用 AI 开发

### 3.1 什么是 Vibecoding

> 开发者用自然语言描述"想要什么"，AI 负责设计、编码、测试、文档的全流程协作范式。

**核心转变**：从"我写代码"变成"我描述意图，AI 执行，我验收"。

### 3.2 我的 Vibecoding 工作流

```
                        ┌────────────┐
                        │  表达意图   │  "我想做一个A股回测系统"
                        └─────┬──────┘
                              ↓
                        ┌────────────┐
                        │  脑暴澄清   │  范围边界、关键决策
                        └─────┬──────┘
                              ↓
                  ┌───────────┴───────────┐
                  ↓                       ↓
           ┌────────────┐          ┌────────────┐
           │  设计规约   │          │  架构决策   │
           │  Spec      │          │  ADR       │
           │  做什么     │          │  为什么    │
           └─────┬──────┘          └────────────┘
                  ↓
           ┌────────────┐
           │  实现计划   │  任务拆分 + 文件清单
           │  Plan      │
           └─────┬──────┘
                  ↓
           ┌────────────┐
           │  TDD 编码   │  先写测试，再写实现
           └─────┬──────┘
                  ↓
           ┌────────────┐
           │  Review    │  AI交叉审查 → 修复 → 验证
           └─────┬──────┘
                  ↓
           ┌────────────┐
           │  完成合并   │
           └────────────┘
```

### 3.3 五个关键实践

#### 实践一：设计先行，不写无计划的代码

**问题**：直接让 AI "写一个回测系统"，代码会散乱、逻辑冲突、无法扩展。

**做法**：每个功能模块先产出两份文档——

- **Spec（设计规约）**：数据流、API 契约、组件关系（11 份）
- **ADR（架构决策）**：为什么选这个方案、不选那个（4 份）

**效果**：Plan 驱动编码，AI 按路线图执行不跑偏；未来回顾有完整上下文。

> 例：组合回测引擎开发前，先写了 15KB 的设计规约，包含账户模型、出场规则、市场环境感知的完整设计，然后再拆成 12 个独立任务逐个实现。

---

#### 实践二：TDD 不可跳过，测试即文档

**问题**：AI 生成的代码不总是符合预期，回测引擎的逻辑错误很难肉眼发现。

**做法**：**每个核心模块都先写测试，再写实现**。

```
真实例子 — 回测引擎开发：

1. test_backtester.py     → 先写 15 个测试用例
   ├── 买入后应有持仓
   ├── T+1 当天不能卖出
   ├── 佣金计算正确
   ├── 卖出收印花税
   ├── 100 股整数倍
   └── ...10 个更多

2. backtester.py          → AI 写实现
3. pytest                 → 15/15 通过 ✅
```

**效果**：

- 25 个测试文件形成安全网，后续重构不引入回归
- 测试用例就是行为文档，新人看测试就懂逻辑
- AI 在有测试约束时生成的代码更准确

---

#### 实践三：插件化架构天然适配 Vibecoding

**问题**：系统需要持续扩展（新策略、新指标、新数据源），如何不影响已有代码？

**做法**：5 类插件接口（DataSource / Indicator / Strategy / Screener / Reporter），全部由 Python 包自动发现。

```
添加新策略 = 只需 2 个文件：
  autotrade/strategies/my_strategy.py    ← 策略逻辑
  config/strategies/my_strategy.yaml     ← 参数配置

无需改任何已有代码，重启自动发现 ✅
```

**效果**：增量开发极其高效，AI 只需关注新文件的正确性，与现有系统零耦合。

---

#### 实践四：子代理并行 + 渐进式重构

**问题**：大任务串行太慢，但一次性全改容易出错。

**做法**：

**并行开发** — 独立任务用多个 AI 子代理同时推进：

```
2026-06-27 同日并行：
├── 子代理 A：组合回测引擎实现
├── 子代理 B：TradingAgents AI 筛选
└── 子代理 C：海龟策略 + 做空支持
```

**渐进式重构** — 大重构分步提取，每步可验证：

```
AI 策略页面重构：
  AIStrategy.tsx (单体 800+ 行)
    ↓ Step 1: 提取 ChatBubble
    ↓ Step 2: 提取 ChatInput
    ↓ Step 3: 提取 ChatMessages
    ↓ Step 4: 提取 ChatSessionList
  AIStrategy.tsx (精简到 ~200 行)
```

---

#### 实践五：系统性调试，不靠猜

**问题**：AI 写的代码出 bug 时，容易陷入"试错循环"——改一下、跑一下、不行再改。

**做法**：遵循固定的调试流程——

1. **复现**：精确复现 bug 的触发条件
2. **诊断**：AI 自主插入诊断日志定位根因
3. **修复**：最小化改动
4. **验证**：跑测试 + 手动验证
5. **清理**：移除诊断代码，提交

**效果**：2026-06-30 的密集修复期，30 个 commits 中大部分在 5-10 分钟内从"发现"到"修好"。

---

## 四、遇到的困难与解决方案

### 困难一：AI 生成的策略代码无法直接运行

**现象**：DeepSeek 生成的 Python 策略类引用了不存在的指标列名、参数类型不匹配。

**根因**：AI 不了解项目内部的指标命名约定（如 `ind_ma_20` 而非 `ma_20`）和参数传递方式。

**解决**：

1. 在 System Prompt 中注入完整的指标 API 文档和列名约定
2. 生成后用 `importlib` 动态加载，捕获 ImportError / TypeError
3. 错误信息直接返回前端展示，用户可让 AI 修复
4. 加了 5 层 fallback 逻辑：参数类型转换 → schema 默认值 → YAML 回退 → 类属性默认值

> **关键教训**：AI 生成代码需要"护栏"——详细的上下文约束 + 多层次的错误兜底。

---

### 困难二：前端 React 迁移的渐进风险

**现象**：原生 JS SPA（`frontend/`）需要整体重写为 React + TypeScript（`web/`），但中途不能影响现有功能。

**解决**：

1. **双前端并存**：旧 `frontend/` 保留为降级回退，新 `web/` 独立开发
2. **API 统一 `/api` 前缀**：消除前后端路由冲突（之前 `/groups` 既可能是前端路由也可能是 API）
3. **SPA fallback 中间件**：基于 `Sec-Fetch-Dest` 头判断是页面导航还是 API 请求
4. 按页面逐个迁移，每迁移一个就关闭旧版对应路由

> **关键教训**：大重构不要"一把梭"，保持新旧并存 + 逐步切换。

---

### 困难三：参数类型漂移

**现象**：策略参数在 YAML 中是字符串（如 `"5"`），传到 Python 构造函数期望 `int`，前端表单又可能传 `float`。参数类型不一致导致策略静默失败。

**解决**：

1. 在策略基类构造函数中统一做 `int()` / `float()` 强制转换
2. 前端 `StrategyParamsEditor` 根据 `param_schema` 的类型元数据正确序列化（int→number, bool→boolean）
3. JSON 数组/对象参数用 `JSON.stringify` + 后端 `_parse_list` 解析
4. 8 个 commit 逐步修复了 `macd_divergence`、`trend_bb_rsi`、`three_gap_up_buy` 等策略的参数类型问题

> **关键教训**：动态类型语言 + 多数据源（YAML/JSON/表单）必须建立**端到端的类型契约**。

---

### 困难四：AI 聊天会话的状态管理

**现象**：多轮对话中，AI 生成策略后用户再发消息，AI 有时"忘记"之前生成的策略代码，导致后续修改基于错误的上下文。

**解决**：

1. `SessionStore` 用 `threading.Lock` 保证线程安全
2. 每条 assistant 消息都持久化 `strategy_code` 和 `strategy_params`
3. 发送新消息时，始终将**最新的策略代码**注入 system prompt 作为上下文
4. 会话 TTL 2 小时 + 后台线程每 30 分钟清理过期会话
5. 前端 `localStorage` 持久化 sessionId，刷新不丢失

> **关键教训**：AI 的"记忆"不可靠，必须显式管理上下文——把最新状态写入 prompt。

---

### 困难五：全市场数据的加载性能

**现象**：选股器和组合回测需要加载全市场 5000+ 只股票的日线数据，首次加载耗时 30 秒+。

**解决**：

1. **本地 Parquet 缓存**：数据一次下载、多次复用，比网络 API 快 100x
2. **并行加载**：`ThreadPoolExecutor(32 workers)` 并行读取 Parquet 文件
3. **策略信号预缓存**：`StrategySignalCache` 一次性预计算所有策略信号到 hash set，后续 O(1) 查询
4. **LLM 响应缓存**：`LLMCache` 持久化 DeepSeek/TradingAgents 的结果，避免重复 API 调用
5. **前端优化**：React.lazy 代码分割 + GZip 压缩 + 搜索 debounce 500ms

> **关键教训**：量化系统的瓶颈在数据——缓存分层设计（本地→内存→预计算）是性能关键。

---

## 五、Vibecoding 心得总结

### 5.1 什么做对了

| 实践                    | 效果                                                |
| ----------------------- | --------------------------------------------------- |
| **Spec + ADR 设计先行** | 11 份设计文档让 AI 执行不跑偏，架构一致性保持 13 天 |
| **TDD 测试驱动**        | 25 个测试文件 = 重构安全网，多次大改后零回归        |
| **插件化架构**          | 添加 7 个新策略、2 个选股器不用改动核心代码         |
| **子代理并行**          | 单日最多 3 个子代理同时推进独立模块                 |
| **调试流程化**          | 30 个 bug 修复平均 5-10 分钟一个                    |

### 5.2 可以做得更好

| 改进点             | 建议                                                                  |
| ------------------ | --------------------------------------------------------------------- |
| **类型安全**       | 如果重来，考虑用 Pydantic 做全链路类型校验，避免 8 个参数类型 commits |
| **集成测试先行**   | 组合回测的集成测试在实现后期才补，导致前期有遗漏的边界情况            |
| **文档同步自动化** | API 文档是手写的，Swagger 自动生成 + 手动补充会更高效                 |
| **Git 提交粒度**   | 调试期的 commits 太碎（20 个 debug commits 在一天内），应该 squash    |

### 5.3 核心认知

> **Vibecoding 不是"AI 替你写代码"，而是"你指挥 AI，AI 执行，你验收"。**
>
> 你的角色从"程序员"变成了"技术总监"——决定做什么、审查做得对不对、判断什么时候算完成。

**三个最重要的技能**：

1. **描述能力** — 能把模糊想法转化为清晰的设计规约
2. **审查能力** — 能快速判断 AI 生成的代码是否符合预期
3. **调试能力** — 能引导 AI 系统性定位和修复问题

---

## 六、项目数据一览

| 维度     | 数据                      |
| -------- | ------------------------- |
| 开发周期 | 13 天                     |
| Git 提交 | 165+ commits              |
| 总文件   | 170+ 个                   |
| 代码总量 | ~28,000 行                |
| 设计文档 | 11 Spec + 11 Plan + 4 ADR |
| 测试文件 | 25 个                     |
| 功能页面 | 8 个                      |
| 内置策略 | 9 个                      |
| 技术指标 | 6 个                      |
| 数据源   | 3 个                      |
| 选股器   | 2 个                      |

---

<p align="center">
  <sub>🧬 Vibecoding — 让想法以代码的速度生长</sub>
</p>
